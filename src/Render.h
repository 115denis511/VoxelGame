#ifndef __RENDER_H__
#define __RENDER_H__

#include "stdafx.h"
#include "RenderResources.h"

namespace engine {
    class Render {
    public:
        static bool init();

    private:

    };
}

#endif